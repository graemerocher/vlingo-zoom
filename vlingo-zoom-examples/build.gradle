buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
}
subprojects { project ->
    apply plugin: "java"

    repositories {
        mavenLocal()
        jcenter()
    }
}